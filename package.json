{
  "name": "lnx-beats-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/js/app.js && node --check public/js/order.js && node --check public/js/admin.js && node --check public/js/site-config.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "helmet": "^8.1.0",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.5"
  },
  "engines": {
    "node": ">=20"
  }
}
